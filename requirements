
Request to Go Up from a floor

Request to Go Down from a floor

Request to stop at a floor

Add a new elevator to the elevator network

disable an elevator in the elevator network

enable the elevator in the elevator network

Adding a floor to the elevator network

Disabling a floor in the elevator network


